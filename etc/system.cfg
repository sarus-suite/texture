ARCH=$(uname -m)

function check_nvidia_gpus() {
  if nvidia-smi &>/dev/null
  then
    NUM_NVIDIA_GPUS=$(/usr/bin/nvidia-smi --list-gpus | wc -l)
  else
    NUM_NVIDIA_GPUS=0
  fi
}
check_nvidia_gpus
